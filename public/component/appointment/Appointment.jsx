import { Grid } from '@carbon/react';
const Appointment = () => {
    return (
        <Grid className='appointment-page'>
            APPOINTMENT PAGE
        </Grid>
    );
};

export default Appointment;
